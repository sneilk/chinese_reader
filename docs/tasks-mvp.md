# Подзадачи MVP

Разбивка [rfc-mvp.md](rfc-mvp.md) на выполнимые куски. Порядок внутри блока — по зависимостям; задачи без общих зависимостей делаются в любом порядке.

Оценки: **S** — до половины дня, **M** — день-два, **L** — больше двух дней.
«Готово, когда» — то, что можно проверить, а не ощущение завершённости.

Сводка: 8 задач в этапе 0, 18 в точке 1, 10 в точке 2, 11 в точке 3, 8 в точке 4.

---

## Этап 0 — ворота (go/no-go для всего плана)

Ничего из этого не выбрасывается при провале: ВМ, аккаунты и фикстуры нужны в любом сценарии. Меняется только слой загрузки.

| ID | Задача | Оценка | Зависит от |
|---|---|---|---|
| ~~**T0.1**~~ ✅ | ВМ в Yandex Cloud: Ubuntu 24.04, 2 vCPU (100%), 4 ГБ, SSD 20 ГБ, публичный IP, группа безопасности 443 и 22 отовсюду (вход только по ключу) | S | — |
| ~~**T0.2**~~ ✅ | Playwright + Chromium + Xvfb на ВМ; headful под Xvfb запускается, доступ к экрану через SSH-туннель (`x11vnc`, порт наружу не открывать) | S | T0.1 |
| ~~**T0.3**~~ ✅ | **Открыть главу 51shucheng с ВМ.** Челлендж пройден автоматически, **ноль ручных вмешательств**; профиль переживает перезапуск браузера и перезагрузку ВМ | M | T0.2 |
| ~~**T0.4**~~ ✅ | Разведка сайта: URL-схема, пагинация, упрощённые иероглифы, готовые `.txt` | S | T0.3 |
| ~~**T0.5**~~ ✅ | HTML-фикстура главы снята; размер главы и число предложений замерены | S | T0.3 |
| ~~**T0.6**~~ ✅ | Yandex Cloud: сервисный аккаунт `chinese-reader-translate`, роль `ai.translate.user`, API-ключ в `/etc/chinese-reader/env`; перевод и глоссарий проверены | S | — |
| ~~**T0.7**~~ ✅ | Дамп БКРС найден, формат определён (DSL), фикстура на 30 статей снята | M | — |
| ~~**T0.8**~~ ✅ | **Ворота пройдены:** ноль ручных вмешательств при пороге «не более одного на 5 глав». Лестница отступления не понадобилась | S | T0.3 |

### Результаты этапа 0 (T0.2–T0.5)

**Ворота пройдены с запасом.** Челлендж решился сам при первом же заходе: HTTP 200, кука `cf_clearance` получена, ручных вмешательств — **ноль** при пороге «не более одного на 5 глав». Профиль пережил и перезапуск браузера, и перезагрузку ВМ. Лестница отступления из §6 концепции не понадобилась.

| Проверка | Итог |
|---|---|
| Первый заход, headful + постоянный профиль | 200, челленджа нет |
| Перезапуск процесса браузера, тот же профиль | 200, челленджа нет |
| Перезагрузка ВМ, профиль с диска | 200, челленджа нет; сервисы поднялись сами |
| **Headless на том же профиле** | **403, челлендж — при живой `cf_clearance`** |

**Headless не работает — это решение, а не наблюдение.** Куки недостаточно: Cloudflare распознаёт headless и отдаёт 403 даже с валидной `cf_clearance` в профиле. Значит Xvfb и headful — постоянный режим работы загрузчика, а не костыль для первого захода. Прямо влияет на T1.3 и на состав ВМ.

**Сайт: 无忧书城.** Схема URL — `/{жанр}/{книга}/{идентификатор}.html`, книга-образец `renwen/zhuguniangjintiandiaokenglemei` (550 глав). Уточнено при выкладке: последний сегмент — это **числовой идентификатор главы, а не её порядковый номер**, и домен боевых ссылок — `www.51shucheng.net`. Попытка собрать адрес «по схеме» даёт честный 404; рабочий адрес лежит в `data/target.json`. Пагинации внутри главы нет — **T1.5 (склейка страниц) в MVP может не понадобиться**, но проверить на второй книге стоит. Ссылок на `.txt` не нашлось: качаем поглавно. Иероглифы упрощённые — 145 маркеров против 0 традиционных, канон B5 подтверждён.

| Замер | Значение |
|---|---|
| Видимый текст главы | **3157 иероглифов** (было предположение 5000) |
| Весь текст из HTML | 5424 иероглифа |
| Абзацев `<p>` | 76 |
| Предложений по терминаторам | ~153, медиана 19 символов |
| Предложений короче 8 символов | 25, то есть 16% |
| Батчей перевода по 10 000 символов | **1** |

Два вывода из замеров. Первый: **около 40% текста в HTML — навигация и списки глав**, поэтому generic-извлечение переводило бы мусор и увеличило счёт примерно в полтора раза — адаптер T1.4 окупается сразу. Второй: правило «склеивать куски короче 8 символов» из RFC §5.2 задевает каждое шестое предложение — то есть оно не косметика, и его надо проверять на снапшотах.

Цифры по предложениям сняты грубым разбором HTML, до нормализации и адаптера: там есть выброс в 5049 символов — это склеенная навигация, а не предложение. Точные значения появятся после T1.4 и T1.7.

### Результаты T0.6 — переводчик

Сервисный аккаунт `chinese-reader-translate` (`ajem2oicvd3rusvbit1m`), роль `ai.translate.user` на каталог, API-ключ лежит в `/etc/chinese-reader/env` с правами `600 root:root`. Биллинг через CLI не виден (нужна роль `billing.accounts.viewer`), но раз ВМ создалась и переводы проходят — платёжный аккаунт активен.

| Проверка | Итог |
|---|---|
| Перевод 3 предложений | 0,45 с |
| Батч из 60 предложений | принят целиком, ответ в том же порядке |
| Имя собственное `江雪明` | транслитерировано верно **без глоссария** |
| 离合词 `帮忙` в разорванной форме | разобран верно |
| Чэнъюй `一见如故` | **переведён неверно** — буквально, мимо смысла |
| Глоссарий `zh→ru` | **поддерживается** |

Два вывода. Первый: **чэнъюй — подтверждённое слабое место**, ровно как предсказывали документы; имена и сепаратные глаголы движок держит сам, так что глоссарий нужен точечно, а не на весь словарь. Второй, важный для этапа 1: на терминах, которые движок и так переводит правильно, **глоссарий задваивает результат** — подстановка добавляется к уже переведённому. Значит в глоссарий главы должны попадать только слова с заведомо неверным дефолтным переводом, а не все слова пользователя. Это меняет формулировку T1 этапа 1: нужен не «собрать глоссарий из личного словаря», а «собрать из тех слов, где пользователь исправил перевод».

### Результаты T0.7 — дамп БКРС

Ежедневная выгрузка одним файлом: `bkrs.info/downloads/daily/dabkrs_<ГГММДД>.gz`, **83,4 МБ** в архиве, формат **DSL**, кодировка UTF-8, направление `Chinese → Russian` объявлено в шапке.

Структура плоская: строка-заголовок без отступа, строки перевода с отступом, в среднем 2 строки на статью. Разметка — закрытый набор тегов: `[m1]`/`[m2]` (уровни списка), `[p]` (пиньинь и пометы), `[ex]` и `[*]` (примеры), `[c]`, `[i]` (оформление). Фикстура на 30 статей — `data/bkrs-sample.dsl`.

**Оценка T2.2 снижена с M–L до M.** Опасение было про «DSL с вложенной разметкой на несколько дней»; по факту вложенности нет, теги перечислимы, а `[ex]`/`[*]` мы всё равно выкидываем на выдаче. Это парсер построчного формата со стриппером тегов. Фолбэк на CC-CEDICT остаётся в запасе, но по текущим данным не понадобится.

**Что делать при провале T0.8.** Ступень 2 — выпускать трафик ВМ через домашний адрес (WireGuard или прокси на домашнем роутере), весь сервис остаётся на ВМ. Ступень 3 — гибрид: загрузчик на ноутбуке отдаёт текст в API. Ступень 4 — расширение для браузера. Ступени 3 и 4 меняют только T1.3 и схему развёртывания; всё остальное в плане остаётся как есть.

### Созданная инфраструктура (T0.1)

| Что | Значение |
|---|---|
| Каталог | `default`, `b1g5qs4tjoe3s8v23g0f` (облако `cloud-seleznevn`) |
| ВМ | `chinese-reader`, `fhmvj6lggvte3tmjechg`, `ru-central1-a`, `standard-v3`, 2 vCPU 100% / 4 ГБ |
| Диск | `chinese-reader-boot`, `network-ssd`, 20 ГБ, образ `ubuntu-2404-lts-oslogin-v20260803` |
| Сеть | `lenta-net` / `lenta-subnet-a` (`e9b1fdko7aik7vs0bobo`), внутренний адрес `10.130.0.11` |
| Группа безопасности | `chinese-reader-sg`, `enpcuq147t65944mpcmi`: ingress 443/tcp и 22/tcp с `0.0.0.0/0`, egress любой |
| Вход | `yc-user`, ключ `id_ed25519`, парольная аутентификация отключена образом |

Публичный IPv4 здесь намеренно не записан: он эфемерный и меняется при пересоздании, а репозиторий — не место для адреса машины с открытым SSH. Актуальный адрес — `yc compute instance get --name chinese-reader`.

Два отступления от исходной формулировки задачи, сделанные осознанно:

- **Порт 22 открыт всем, а не «с домашнего адреса».** Текущий внешний адрес рабочей машины — IPv6 из корпоративной сети, в подсети ВМ IPv6 нет вовсе, а домашний адрес динамический. Привязка к адресу дала бы иллюзию защиты и реальный риск потерять доступ. Защита — ключевая аутентификация; парольный вход в образе отключён.
- **Заведена отдельная группа безопасности.** Дефолтная в этой сети разрешает вход с `0.0.0.0/0` по любому протоколу, и три соседние ВМ работают именно с ней. Наша ВМ — единственная в каталоге с ограниченным набором портов.

**T0.5 и T0.7 определяют оценку остального.** Если глава окажется вдвое больше — вырастет число батчей и цена; если дамп БКРС окажется в DSL с вложенной разметкой — T2.2 превращается из M в L.

---

## Точка 1 — «глава грузится и читается»

Демонстрируется тем, что глава открывается по URL на телефоне и перевод предложения показывается по тапу. Карточки и словаря ещё нет.

### Каркас

| ID | Задача | Готово, когда | Оценка | Зависит от |
|---|---|---|---|---|
| ~~**T1.1**~~ ✅ | Каркас репозитория: `backend/` (FastAPI, ruff, pytest, pre-commit, Python 3.12), `web/` (Vite + React + TS), `data/` в `.gitignore` | `pytest` и `ruff` проходят, оба сервера поднимаются, фронт достаёт `/api/health` через прокси | S | — |
| ~~**T1.2**~~ ✅ | SQLAlchemy 2.0 + Alembic; первая миграция: `sources`, `documents`, `chapters`, `sentences` | накат → откат → повторный накат покрыты тестом; ограничения схемы проверены отдельно | S | T1.1 |

### Конвейер

| ID | Задача | Готово, когда | Оценка | Зависит от |
|---|---|---|---|---|
| ~~**T1.3**~~ ✅ | `Fetcher` + `BrowserFetcher`: persistent-профиль, таймауты и ретраи из таблицы RFC §4, классификация отказов (`challenge`, `not_found`, `fetch_timeout`) | на ВМ: живая глава 200 за 4,8 с и 107 КБ; битый URL → `not_found`; пауза между запросами соблюдается | L | T0.3 |
| ~~**T1.4**~~ ✅ | `SiteAdapter` + `adapters/51shucheng` + generic-фолбэк на `trafilatura`; распознаёт «мягкий 404» по отсутствию `#neirong` | вживую: 72 абзаца и 2922 иероглифа, отсечено ~2500 иероглифов навигации; страница оглавления → `empty_extract` при HTTP 200 | M | T0.5, T1.1 |
| ~~**T1.5**~~ ✅ | Склейка пагинированной главы: потолок 20 страниц, задержка 2 с, при превышении — `failed` + `adapter_error` | двухстраничная глава склеена в одну запись, на 21-й — `adapter_error`; кольцо ссылок не зацикливает, относительный `next_url` разрешается. Пагинации на 51shucheng нет (T0.4), поэтому проверено на подставном адаптере | S | T1.4 |
| ~~**T1.6**~~ ✅ | Нормализация в канон: zero-width и экзотические пробелы, отступы, повторы пробелов; полноширинная пунктуация сохраняется (NFC, не NFKC) | на живой главе: 72 абзаца, 3605 символов, невидимых символов 0 | S | T1.4 |
| ~~**T1.7**~~ ✅ | Резка на предложения по правилам RFC §5.2 (терминаторы, прилипание закрывающих кавычек, склейка кусков < 8 символов внутри абзаца) | 132 предложения на живой главе, склеено 19 кусков из 151; офсеты режут канон обратно, предложения не пересекаются | M | T1.6 |
| ~~**T1.8**~~ ✅ | Сегментация: jieba + CC-CEDICT в userdict, `tokens_json` c офсетами и `kind` | на живой главе: 2575 токенов за 7 мс, офсеты режут канон, покрытие сплошное; золотой набор на 25 предложениях | M | T1.6 |
| ~~**T1.9**~~ ✅ | `Translator` + `YandexTranslate`: батчи ≤10 000 символов, сохранение порядка, подсчёт `chars_sent`, обработка 429 и таймаутов | 18 тестов на записанных ответах; **на живом API глава переведена целиком** — 132 предложения из 132, 3534 символа, 1,77 ₽, расход записан в журнал | M | T0.6, T1.7 |
| ~~**T1.10**~~ ✅ | Конвейер как фоновая задача + статусная модель (`fetching → segmented → translating → ready`, `failed`) и `error_kind` | отказ переводчика оставляет главу на `segmented` и читаемой — тест; на фикстуре главы весь путь до `ready`: 132 предложения, 2575 токенов, один запрос | M | T1.3, T1.8, T1.9 |
| ~~**T1.11**~~ ✅ | Потолок расходов: мягкий лимит символов на главу и на месяц в конфиге, отказ с внятным сообщением при превышении | 12 тестов: оба лимита останавливают отправку до сети, глава остаётся читаемой, причина отличима от сбоя провайдера (`budget_exceeded`) | S | T1.9 |

### API

| ID | Задача | Готово, когда | Оценка | Зависит от |
|---|---|---|---|---|
| ~~**T1.12**~~ ✅ | `POST /api/chapters` (202, идемпотентен по URL), `GET /api/chapters/{id}` (глава целиком), `POST /api/chapters/{id}/translate` | повторный POST в сеть не ходит, глава в `failed` перезапускается; happy path и все `error_kind` покрыты — 21 тест | M | T1.10 |

### Клиент

| ID | Задача | Готово, когда | Оценка | Зависит от |
|---|---|---|---|---|
| ~~**T1.13**~~ ✅ | Каркас фронта: `api.ts`, три маршрута, адаптивный layout, тёмная тема, крупный дефолтный шрифт | в мобильном вьюпорте 390×844 и на десктопе, обе темы: горизонтальной прокрутки нет, контраст ≥5,7:1, ошибок в консоли ноль. На живом устройстве — при развёртывании (T1.17) | S | T1.1 |
| ~~**T1.14**~~ ✅ | Экран ввода URL: поле, отправка, опрос статуса, человеческие сообщения по каждому `error_kind` | вживую в браузере: все шесть причин дают разные сообщения, кнопка повтора есть там, где повтор осмыслен; при `translate_failed` и `budget_exceeded` текст главы остаётся на экране | S | T1.12, T1.13 |
| ~~**T1.15**~~ ✅ | **Рендер главы:** спаны по токенам, один контейнер, делегирование по `data-s`/`data-e`; тап — слово, drag — расширение в пределах предложения, долгий тап — символ | 2504 спана в 72 абзацах, кадр 8,3 мс при прокрутке, отклик на тап 8,3 мс; протяжка упирается в границу предложения, долгий тап даёт символ; склейка спанов равна канону, офсеты режут его обратно | L | T1.12, T1.13 |
| ~~**T1.16**~~ ✅ | Нижняя панель: перевод предложения по тапу, чтение не прерывается, модалок нет | вживую: тап даёт перевод при **нуле сетевых запросов**, панель не перекрывает последний абзац, протяжка и долгий тап меняют её содержимое | S | T1.15 |

**Жесты переразложены после чтения вживую.** Приёмка T1.15 и T1.16 выше описывает первую редакцию: любое касание открывало панель на треть экрана. На настоящей главе это оказалось дорого — незнакомое слово встречается в каждом втором предложении, и чтение прерывалось на каждом. Сейчас ответ соразмерен жесту: тап только подсвечивает, удержание даёт подсказку у пальца (слово, чтение, значение) и гаснет при отпускании, панель со статьями и кнопкой «в словарь» открывает двойной тап, протяжка — фраза. Сужение до одного знака переехало из долгого тапа в ряд знаков под заголовком панели. Правила — [segmentation.md](segmentation.md) §4; проверено в браузере на 390×844, 23 проверки.

### Развёртывание

| ID | Задача | Готово, когда | Оценка | Зависит от |
|---|---|---|---|---|
| ~~**T1.17**~~ ✅ | Домен, A-запись, Caddy: автоматический Let's Encrypt + basic auth | на поддомене `sslip.io`: сертификат Let's Encrypt выпущен, без пароля 401, с HTTP редирект 308 на HTTPS. Домена не было, поэтому взят sslip.io — **имя привязано к IP, при пересоздании ВМ сменится** | S | T0.1 |
| ~~**T1.18**~~ ✅ | systemd-юнит, `EnvironmentFile` с правами 0600, скрипт выкладки; `data/` не затирается | выкладка прогнана: миграции накатились, сервис активен и в автозапуске, профиль браузера 12 МБ и база на месте. Юнит требует своего `HOME` — под `ProtectHome` Chromium не стартует вовсе | M | T1.17, T1.12 |

**Демонстрация точки 1:** глава читается на телефоне через боевой домен, перевод предложения по тапу.

---

## Точка 2 — «карточка и словарь»

| ID | Задача | Готово, когда | Оценка | Зависит от |
|---|---|---|---|---|
| ~~**T2.1**~~ ✅ | Импортёр CC-CEDICT в `dict_entries` (вместе с миграцией таблицы — импортировать было некуда) | 124 769 статей в базе, пиньинь переведён в диакритику на импорте | S | T1.2 |
| ~~**T2.2**~~ ✅ | Импортёр БКРС из DSL: построчный разбор, стриппер тегов `[m1] [m2] [p] [c] [i]`, отбрасывание `[ex]`/`[*]` | **на ВМ импортировано 3 305 443 статьи**; парсер сверен с живой выборкой T0.7 — чтение оказалось отдельной строкой, а не в `[p]`. Выдача: БКРС идёт первым, `花姐` получило настоящую статью вместо разбора по знакам | M | T0.7, T1.2 |
| **T2.3** ⊘ | ~~FTS5~~ **отложен как неоправданный**, замер ниже. Фильтр на выдаче (иероглифы, ≤4 знаков) сделан в T2.4; таблица `dict_entries` заведена в T2.1 | критерий «миллисекунды» выполняет обычный индекс: 0,091 мс на слово при 124 769 статьях | S | T2.1 |
| ~~**T2.4**~~ ✅ | `GET /api/lookup`: значения из локальных словарей, порядок источников БКРС → CC-CEDICT | известное слово, разбор по знакам для имени героя, внятное «нет» на латинице; 11 тестов | S | T2.1 |
| ~~**T2.5**~~ ✅ | Карточка в UI: пиньинь, значения, перевод предложения; фолбэк при отсутствии статьи — значения и чтения отдельных иероглифов с явной пометкой. `pypinyin` **не понадобился**: чтения знаков берутся из их же словарных статей, то есть точнее автоматических, и зависимость не добавлена | вживую: тап по `花姐` даёт разбор по знакам с чтением и пометкой «статьи на это слово нет», тап по `乌云` — статью с пиньинем | M | T2.4, T1.16 |
| ~~**T2.6**~~ ✅ | Миграция `user_words` (со своими `user_translation`, `note`), `contexts`, `word_occurrences`; `POST /api/words` | слово сохраняется с предложением и офсетами, офсеты обязаны резать предложение обратно в него; контекст переживает удаление главы — 21 тест | S | T1.2 |
| ~~**T2.7**~~ ✅ | Правка границ: склейка и сужение выделения, сохранение результата в `user_words` и подкладка в userdict jieba | правка переживает перезапуск (тест на новом экземпляре с файла), частота читателя побеждает словарную; вживую сохранены слово и фраза, офсеты режут предложение обратно в них | M | T2.6, T1.15 |
| ~~**T2.8**~~ ✅ | Экран словаря: список (слово, пиньинь, значение, контекст, дата), `PATCH` своих полей, `DELETE` ошибочной записи | вживую: карточки с подсвеченным контекстом, правка по месту, поиск по своему переводу, удаление | M | T2.6 |
| **T2.9** ◐ | Бэкап: `sqlite3 .backup`, профиль браузера, ротация, проверка целостности плюс **ночной таймер systemd — работают на ВМ** (пробный запуск: копия 445 МБ, `integrity_check: ok`, следующий запуск 04:00). Осталось: выгрузка `rclone` в Object Storage, отдельный сервисный аккаунт на бакет и восстановление на пустой ВМ | восстановление на пустой ВМ не проверялось | M | T1.18 |
| ~~**T2.10**~~ ✅ | Диагностика в UI по всем `error_kind` (T1.14) плюс экран состояния сервиса: что настроено, а что нет, с советом по каждому пункту; логи в stdout → journald | вживую: экран верно указал на отсутствие БКРС, ключа, профиля и на headless-режим | S | T1.14 |

**Приёмка:** критерии из [rfc-mvp.md](rfc-mvp.md) §2 — глава дочитана на телефоне, ≥30 слов с контекстом, ноль ручных правок в SQLite.

---

## Точка 3 — английский режим

Второй язык из решения A5 концепции. Разбор решений — [english.md](english.md).

Главное, что определило объём: язык стал **свойством главы**, а не режимом приложения. Общими остались загрузка, нормализация, офсеты, статусы, панель, личный словарь и экран состояния; языковых мест оказалось три — токены, предложения, направление перевода.

| ID | Задача | Готово, когда | Оценка | Зависит от |
|---|---|---|---|---|
| ~~**T3.1**~~ ✅ | `chapters.lang` и `chapters.next_chapter_url`, таблица `speech_usage`; язык объявляет адаптер, generic определяет по тексту | накат → откат → повторный накат проходят, индекс и оба CHECK на месте после пересоздания таблицы | S | T1.2 |
| ~~**T3.2**~~ ✅ | Токенизация английского регуляркой, без словаря: слова с апострофом, числа, переводы строк отдельными токенами | покрытие сплошное на девяти видах ввода, офсеты режут текст обратно; 23 теста | S | T1.8 |
| ~~**T3.3**~~ ✅ | Английские правила резки на предложения: сокращения, инициалы, дробные числа, реплика с восклицанием внутри фразы; порог огрызка в словах | `Mr.`, `e.g.`, `3.15`, `J. R. R.` и `"Go now!" he shouted.` не режутся; одна строка режется по-разному в зависимости от языка | M | T1.7 |
| ~~**T3.4**~~ ✅ | Адаптер `novelarrow`: структурный поиск блока главы, ссылка «следующая глава», порог пустоты в словах | на синтетической фикстуре: 7 абзацев, шапка и «похожие книги» отсечены, ссылка вперёд отличается от ссылки назад; 26 тестов | M | T1.4 |
| ~~**T3.5**~~ ✅ | Направление перевода из главы: `en→ru` тем же клиентом, направление в журнале расходов | подставной переводчик получает `source="en"` на английской главе; 432 теста целиком зелёные | S | T1.9 |
| ~~**T3.6**~~ ✅ | Обход книги по ссылке «следующая глава»: потолок, остановка на первом отказе, перешагивание загруженного | цепочка из трёх глав загружена одним запросом; повтор «ещё пять» не ходит на сайт за уже загруженным | M | T3.4 |
| ~~**T3.7**~~ ✅ | Англо-русский словарь: импорт DSL поверх разбора БКРС, транскрипция в `reading`, заголовки в нижний регистр; перебор форм слова при поиске | `counted` → статья `count` на живом API; точная форма опережает лемму (`saw` — пила); 21 + 11 тестов | M | T2.4 |
| ~~**T3.8**~~ ✅ | Озвучка русского перевода: SpeechKit, кэш по содержимому, отдельный потолок расходов, отдача файлом с Range | повтор играется из кэша и не пишется в журнал; смена текста перевода даёт новый файл; отказ провайдера не оставляет обрывка в кэше; 13 тестов | M | T1.16 |
| ~~**T3.9**~~ ✅ | Фронт: переключатель «оригинал ⇄ перевод» с памятью, вид перевода по тем же абзацам, озвучка с подсветкой фразы, переход к следующей главе, поле «и ещё N глав» | вживую на 390×844: оба вида, карточка `counted` с пометкой начальной формы, отказ озвучки объясняет себя, жесты переживают переключение туда-обратно, ноль ошибок в консоли | L | T3.5, T3.8 |
| **T3.10** ⊘ | **Заход на живой novelarrow с ВМ**: фикстура, схема URL, вид ссылки «следующая», поведение на несуществующей главе | не сделано — нужен доступ с машины; открытые пункты в [sources.md](sources.md) §4 | S | T3.4 |
| **T3.11** ⊘ | **Ключ SpeechKit и роль `ai.speechkit-tts.user`**; проверка `en→ru` на живом API переводчика | не сделано — нужны ключи в `/etc/chinese-reader/env` | S | T3.8 |

**Что осталось непроверенным.** Всё, что требует живого сайта и живых ключей: адаптер novelarrow работает по синтетической фикстуре, а `en→ru` и синтез — по подставным клиентам. Сводка в [english.md](english.md) §10.

**Демонстрация точки 3:** английская глава открывается по ссылке, читается по словам с карточками из англо-русского словаря, переключается в перевод и читается вслух; книга догружается на N глав вперёд одним запросом.

---

## Точка 4 — по итогам разбора архитектуры

Не новые возможности, а долги, которые обход книги сделал дорогими, и одна поломка без выхода. Разбор — в истории обсуждения; здесь то, что из него сделано.

| ID | Задача | Готово, когда | Оценка | Зависит от |
|---|---|---|---|---|
| ~~**T4.1**~~ ✅ | Уборка после перезапуска: `fetching` → `failed` с причиной `interrupted`, `translating` → `segmented` | глава, застигнутая выкладкой, чинится обычным повтором, а не остаётся вечным спиннером; 8 тестов | S | T1.10 |
| ~~**T4.2**~~ ✅ | Условный запрос главы: `ETag` и 304 на неизменившееся | замерено в браузере: по сети 300 байт заголовков вместо 8511 байт тела, клиентского кода не потребовалось; 7 тестов | S | T1.12 |
| ~~**T4.3**~~ ✅ | `documents.key` вместо перебора соседей по префиксу; засыпка по существующим главам | накат на боевой базе проставил ключи двум книгам; накат → откат → накат чист; 3 теста | S | T3.6 |
| ~~**T4.4**~~ ✅ | Место главы в книге (`chapters.idx`) и ручки `GET /api/books`, `GET /api/books/{id}/chapters` | цепочка из трёх глав получила номера 0, 1, 2; глава, открытая отдельной ссылкой, остаётся без номера и уходит в конец списка; 17 тестов | M | T4.3 |
| ~~**T4.5**~~ ✅ | Экран «Книги» и оглавление; переход в главу из списка | вживую на 390×844: список книг, оглавление с номерами, переход в главу, нечитаемые главы приглушены и помечены причиной | M | T4.4 |
| ~~**T4.6**~~ ✅ | Потолок кэша озвучки, уборка в ночном таймере | давно не звучавшее выбрасывается, пока кэш не влезет в потолок; выброшенное синтезируется заново; 6 тестов | S | T3.8 |
| ~~**T4.7**~~ ✅ | Индекс главы перестаёт пересобираться на каждый опрос | зависимости мемоизации — канон и номер главы, а не массивы из разбора ответа | S | T3.9 |
| ~~**T4.8**~~ ✅ | Живая проверка синтеза на экране состояния; потолок обхода из `/api/health`; error boundary; `npm audit fix` | «Проверить голосом» показывает настоящий ответ провайдера; предел обхода задан на сервере и только там; 0 уязвимостей | S | T3.8 |

**Что осталось прежним.** Ключ к книге выведен из адреса, а не из содержимого сайта: `/{жанр}/{книга}/{номер}` без последнего сегмента. Сайт с плоской схемой адресов сольёт всё в одну книгу — но такого среди целевых нет, а гадать про будущие мы не станем.

---

## Что можно делать параллельно

- **T0.6** (Yandex Cloud, переводчик) и **T0.7** (БКРС) не зависят от челленджа — делаются, пока идёт возня с T0.3.
- **T1.1–T1.2** (каркас, миграции) не зависят от этапа 0 вообще.
- **T2.1–T2.4** (словари) не зависят от точки 1 — можно готовить, пока делается рендер.
- **T1.15** (рендер) — самая тяжёлая задача точки 1; начинать её стоит на моковых данных, не дожидаясь T1.12.

## Порядок, если времени мало

Минимальная цепочка до первой пользы: T0.1 → T0.2 → T0.3 → T1.1 → T1.2 → T1.3 → T1.4 → T1.6 → T1.7 → T1.8 → T1.13 → T1.15. На этом месте глава уже читается локально с сегментацией — без перевода, API и развёртывания. Дальше по приоритету: перевод (T1.9, T1.16), словарь (T2.1, T2.4, T2.5), развёртывание (T1.17, T1.18).
