/**
 * Экран личного словаря.
 *
 * Маршрут заведён в каркасе (T1.13), чтобы навигация была цельной с самого
 * начала. Содержимое появится в T2.8 — вместе с ручками `/api/words`.
 */

export function WordsScreen() {
  return (
    <>
      <h1>Словарь</h1>
      <p className="muted">
        Слова, сохранённые при чтении, появятся здесь: слово, пиньинь, значение,
        контекст и дата.
      </p>
    </>
  )
}
